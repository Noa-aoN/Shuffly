<%# Toast通知パーシャル
    使用方法:
    - サーバー側: flash[:notice] または flash[:alert] を設定
    - JavaScript: window.showToast("メッセージ") を呼び出し
%>
<div id="toast"
     role="status"
     aria-live="polite"
     data-controller="toast"
     data-notice="<%= flash[:notice] %>"
     data-alert="<%= flash[:alert] %>"
     class="fixed left-1/2 -translate-x-1/2 bg-white text-black border border-gray-300 shadow-lg text-sm md:text-lg opacity-0 transition-opacity duration-300 rounded-xl inline-block max-w-[90vw] w-auto whitespace-pre-wrap text-center p-4 box-border z-[1000]"
     style="top: 100px;">
</div>
