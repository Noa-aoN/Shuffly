<h1 class="text-2xl font-bold mb-4">マイページ</h1>

<p>ようこそ、<%= @user.name || @user.email %></p>

<h2 class="mt-6 text-lg font-semibold">あなたの作成したイベント</h2>
<ul>
  <% @events.each do |ev| %>
    <li><%= link_to ev.title.presence || "イベント #{ev.id}", event_path(ev) %> — <%= ev.created_at.to_date %></li>
  <% end %>
</ul>