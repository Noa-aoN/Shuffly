<div class="max-w-6xl mx-auto px-6 py-6 md:py-8">
  <div class="bg-white rounded-xl shadow-sm px-6 py-6 md:py-8 text-center max-w-4xl mx-auto">
    <% if @just_signed_up %>
      <%# サインアップ後の表示 %>
      <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
        <%= image_tag 'icons/person_24dp_1F1F1F_FILL0_wght400_GRAD0_opsz24.svg', width: 32, height: 32, class: "text-green-600", alt: "アカウント登録" %>
      </div>
      <h1 class="text-2xl font-bold text-gray-900 mb-4">アカウント登録が完了しました</h1>
    <% else %>
      <%# ログイン後の表示 %>
      <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
        <%= image_tag 'icons/login_24dp_1F1F1F_FILL0_wght400_GRAD0_opsz24.svg', width: 32, height: 32, class: "text-green-600", alt: "ログイン" %>
      </div>
      <h1 class="text-2xl font-bold text-gray-900 mb-4">ログインしました</h1>
    <% end %>

    <p class="text-gray-600 mb-8">
      作成中のイベントをアカウントに紐付けて保存します。<br>
      よろしいですか？
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center items-stretch">
      <%= render 'shared/button',
            button_text: 'イベントを保存する',
            path: '#',
            variant: 'primary',
            size: 'lg',
            icon: 'icons/save_24dp_1F1F1F.svg',
            icon_class: 'inline-block',
            is_button: true,
            data: { token: @pending_token },
            id: 'btn-save-pending' %>

      <%= render 'shared/button',
            button_text: '破棄してマイページへ',
            path: mypage_path,
            variant: 'tertiary',
            size: 'md',
            icon: 'icons/delete_24dp_1F1F1F_FILL0_wght400_GRAD0_opsz24.svg',
            icon_class: 'inline-block',
            extra_class: 'w-full sm:w-auto lg:min-w-[310px]' %>
    </div>
  </div>
</div>

<%= javascript_include_tag "events_linking", type: "module", defer: true %>
