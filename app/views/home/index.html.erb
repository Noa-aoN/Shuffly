<h1 class="text-2xl font-bold text-center mt-6 mb-8">Shuffly - グループ分けアプリ - </h1>

<div class="max-w-md mx-auto space-y-6">

  <!-- イベント作成セクション -->
  <div class="bg-white rounded p-6 shadow space-y-4">
    <h2 class="text-xl font-semibold text-center mb-4">イベント作成</h2>

    <!-- ゲスト用ボタン -->
    <%= link_to "新しいイベントを作る（ゲストOK）", new_event_path, class: "block w-full text-center bg-green-500 text-white px-4 py-3 rounded hover:bg-green-600 transition" %>

    <!-- ログインユーザー用ボタン -->
    <% if !user_signed_in? %>
      <%= link_to "ログインしてイベントを作成・確認する", new_user_session_path, class: "block w-full text-center bg-blue-500 text-white px-4 py-3 rounded hover:bg-blue-600 transition" %>
    <% else %>
      <%= link_to "イベントを作成・確認する", new_event_path, class: "block w-full text-center bg-blue-500 text-white px-4 py-3 rounded hover:bg-blue-600 transition" %>
    <% end %>
  </div>

</div>